{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debugging",
            "type": "node",
            "request": "launch",
            "program": "${relativeFile}", // Path to file to debug
            "skipFiles": ["<node_internals>/**"],
            "runtimeArgs": ["-r", "ts-node/register"]
        }
    ]
}
